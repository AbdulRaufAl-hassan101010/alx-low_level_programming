#include "main.h"i;

/**
 * _isupper - check for uppercase
 * @c: takes character as input
 * Return: 1 for  if char is a uppercase
 * 0 for lowercase
 */

int _isupper(int c)
{
	return (c > 64 && c < 91);
}
