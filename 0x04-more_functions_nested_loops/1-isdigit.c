#include "main.h"i;

/**
 * _isdigit - check for uppercase
 * @c: expecting  didgit as input
 * Return: 1 for  if char is a uppercase
 * 0 for lowercase
 */

int _isupper(int c)
{
if (c < 48 || c > 57)
{i
	return (0);
}

	return (1);
}
